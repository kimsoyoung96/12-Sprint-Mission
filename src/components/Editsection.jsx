const Editsection = ({ topChlid, center, bottonChild }) => {
  return (
    <div>
      <div>{topChlid}</div>
      <div>{center}</div>
      <div>{bottonChild}</div>
    </div>
  );
};

export default Editsection;
